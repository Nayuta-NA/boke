# Resources 功能使用指南

## 📚 功能概述

学习资源页面现已完全接入后端数据库，支持资源的动态加载、新建和删除功能。

## 🎯 主要功能

### 1. 查看资源列表
- 访问路径：`/frontend/resources`
- 所有资源从后端数据库动态加载
- 按创建时间倒序显示（最新的在前）
- 每个资源卡片显示：图标、标题、描述、访问链接

### 2. 新建资源
**操作步骤：**
1. 点击页面右上角的"新建资源"按钮
2. 在弹出的表单中填写以下信息：
   - **资源标题**（必填）：资源的名称
   - **资源描述**（必填）：简要介绍资源内容
   - **资源链接**（必填）：资源的 URL 地址
   - **图标类名**（可选）：Font Awesome 图标类名
3. 点击"确定"按钮提交

**注意事项：**
- 如果 URL 没有协议前缀，系统会自动添加 `https://`
- 图标未指定时会使用默认的链接图标
- 提交成功后会自动刷新列表

### 3. 删除资源
**操作步骤：**
1. 找到要删除的资源卡片
2. 点击卡片右下角的红色删除按钮（垃圾桶图标）
3. 在确认对话框中点击"确定删除"

**安全提示：**
- 删除操作不可恢复，请谨慎操作
- 系统会要求二次确认以防止误删

## 🔧 技术细节

### 后端 API
```
GET    /api/db/resources      # 获取所有资源
POST   /api/db/resources      # 创建新资源
DELETE /api/db/resources/:id  # 删除资源
```

### 数据存储
- 数据文件：`backend/data/resources.json`
- ID 格式：UUID（确保唯一性）
- 时间格式：ISO 8601

### 前端状态管理
使用 Pinia Store (`frontend.ts`) 统一管理资源状态:
- `loadResources()` - 加载资源列表
- `addResource()` - 添加新资源
- `deleteResource()` - 删除资源

## 📝 示例数据

初始数据包含 6 个常用学习资源:
1. MDN Web Docs - Web 开发权威文档
2. Vue.js 官方文档 - Vue 3 完整指南
3. React 官方文档 - React 库学习资源
4. TypeScript 文档 - 类型安全的 JavaScript
5. Tailwind CSS - 实用优先的 CSS 框架
6. Wepback - 模块打包工具

## 🎨 UI 特性

### 响应式设计
- **桌面端**: 网格布局，每行显示多个资源卡片
- **移动端**: 单列布局，优化触摸操作

### 交互效果
- 卡片悬停时有阴影和位移动画
- 访问按钮悬停时变色并放大
- 删除按钮危险样式提示
- 空状态引导用户添加第一个资源

## ⚠️ 注意事项

1. **数据备份**: 建议定期备份 `resources.json` 文件
2. **权限控制**: 当前版本无权限验证，后续可添加管理员模式
3. **URL 验证**: 系统仅自动添加协议前缀，不验证 URL 有效性
4. **图标选择**: 需要熟悉 Font Awesome 图标库

## 🚀 快速开始

### 确保后端服务运行
```bash
cd backend
npm run dev
```

### 启动前端服务
```bash
npm run dev
```

### 访问页面
打开浏览器访问：`http://localhost:5173/frontend/resources`

## 🛠️ 故障排查

### 问题：资源列表为空
**解决方案：**
1. 检查后端服务是否启动
2. 打开浏览器开发者工具查看网络请求
3. 确认 API 端点 `/api/db/resources` 返回数据

### 问题：无法添加资源
**解决方案：**
1. 检查必填字段是否完整
2. 查看浏览器控制台错误信息
3. 确认后端服务正常运行

### 问题：删除失败
**解决方案：**
1. 检查网络连接
2. 确认资源 ID 是否正确
3. 查看后端日志

## 📞 技术支持

如有问题或建议，请查阅项目文档或联系开发团队。
